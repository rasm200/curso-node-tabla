# Notas :
Este es mi primer programa en NodeJs

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Indica el número a multiplicar             [número] [requerido]
  -l, --listar   Indica si la tabla se lista o no    [booleano] [defecto: false]
  -h, --hasta    Indica el número final por el que se debe multiplicar
                                                          [número] [defecto: 12]
```